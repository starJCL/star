<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.star.dao.ly.xuiPassword">
	<update id="xuiP">
	UPDATE `user` SET `password`=#{password} WHERE `password`=#{password1} AND phone =#{phone} ;
	</update>	
</mapper>