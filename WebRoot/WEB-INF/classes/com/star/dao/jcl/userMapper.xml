<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.star.dao.jcl.user">
	<select id="list" resultType="user">
		select * from user
	</select>
	<update id="xiumi">
		UPDATE `user` SET `password`=#{mi} WHERE phone = #{phone}
	
	</update>
	


</mapper>